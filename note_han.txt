目录

Feature_generation：
    Mol2Vec：
        mol2vec:
           features.py
           helpers.py
        model_300dim.pkl
        Mol2Vec.py  #生成化合物的特征矩阵 python Mol2Vec.py --dataset dataname
    ProtTrans:
        generator.py  #生成蛋白质的特征矩阵 python Mol2Vec.py --dataset dataname

Predictions： #提供训练模型和脚本来预测用户提交的化合物库和蛋白质库之间的cpi
Pretrain_models: #在三个数据集的4个场景中训练保存的模型
Source_Data: #paper的实验数据

Train：
    ColdstartCPI:
        ablation:  #消融实验及变体
        Results:
        dataset.py  #数据处理 数据加载、预处理及划分逻辑（如训练集、验证集、测试集分割）
        model.py
        train_BindingDB.py
        train_BindingDB_missing.py #评估模型在数据稀缺（scarce data）条件下的性能
        train_BindingDB_AIBind.py
        train_BindingDB_AIBind_missing.py #评估模型在数据稀缺（scarce data）条件下的性能
        train_BioSNAP.py
        train_BioSNAP_missing.py #评估模型在数据稀缺（scarce data）条件下的性能